# CREAR EL VOLCADO
**Creamos una carpeta para la práctica**
```bash
mkdir -p $HOME/docker-dumps
```
```bash
cd $HOME/docker-dumps
```
**Creamos un fichero y comprobamos si tiene permisos de lectura:**
```bash
sudo nano dump.sql
```
```bash
ls -l $HOME/docker-dumps/dump.sql
```


